# 手順書1：Client list に Price Group / Markup % 列を追加する

このドキュメントは、開発用 Client Information スプレッドシートの `Client list` シートに **W列（Price Group）** と **X列（Markup %）** を追加するための、Manabu さん向け作業手順書です。

- **想定所要時間**：**10〜15分**
- **対象スプレッドシート**：開発用 Client Information（ID: `1Jqmqs-FVmhXrG7GqPbh6bkvaRWHZXtAEHUsWkZV4f8o`）
- **使用スクリプト**：`apps-script/client-info-dev/setup_price_group.js`
- **冪等性**：何度実行してもOK（途中で失敗しても再実行で復旧可能）

---

## 0. 事前チェック（30秒）

作業を始める前に、以下を確認してください。

- [ ] 開発用スプレッドシートであること（**本番ではない**ことを確認）
  - URLに `1Jqmqs-FVmhXrG7GqPbh6bkvaRWHZXtAEHUsWkZV4f8o` が含まれていればOK
- [ ] 自分以外に編集している人がいないこと（右上のアイコンを確認）
- [ ] `Client list` シートが存在すること

> ⚠ 本番 Client Information（ID: `1CG07N6tYpIoPD_vp0cQ8lu_uMAVO4NRwuvL_J6-fTe8`）には**絶対に実行しないでください**。

---

## ステップ1：開発用 Client Information を開く（1分）

1. ブラウザで以下のURLを開きます：
   ```
   https://docs.google.com/spreadsheets/d/1Jqmqs-FVmhXrG7GqPbh6bkvaRWHZXtAEHUsWkZV4f8o/edit
   ```
2. シート名タブの中に **`Client list`** があることを確認します。

**チェックポイント**：
- [ ] 開発用ブックが開けた
- [ ] `Client list` タブが見える

---

## ステップ2：Apps Script エディタを開く（30秒）

1. メニュー **拡張機能 → Apps Script** を選択します。
2. 新しいタブでスクリプトエディタが開きます。

**チェックポイント**：
- [ ] スクリプトエディタが別タブで開いた

---

## ステップ3：スクリプトを貼り付ける（2分）

1. 左サイドバーの **ファイル** 一覧で、既存の `コード.gs`（または `Code.gs`）を選びます。
2. エディタ内のコードを**すべて削除**します。
3. 以下のローカルファイルの中身をすべてコピーして貼り付けます：
   ```
   apps-script\client-info-dev\setup_price_group.js
   ```
4. 右上の **保存ボタン（フロッピーマーク）** をクリック、または `Ctrl + S` で保存します。
5. プロジェクト名を聞かれた場合は `Price Group Setup` などわかりやすい名前にします。

**チェックポイント**：
- [ ] スクリプトが貼り付けられた
- [ ] 保存マークが消えた（保存完了）

---

## ステップ4：現状を確認する（任意・1分）

実行前に、現在の `Client list` の状態を確認しておきます（**任意ですが推奨**）。

1. エディタ上部の関数選択プルダウンで **`checkClientListStatus`** を選びます。
2. **実行** ボタンをクリックします。
3. 初回実行時のみ「承認が必要」と表示されます：
   - **権限を確認** をクリック
   - 自分のGoogleアカウントを選択
   - 「このアプリは Google で確認されていません」と出た場合は、**詳細 → 安全でないページに移動** をクリック
   - **許可** をクリック
4. 下部の **実行ログ** に以下が表示されることを確認：
   ```
   シート名: Client list
   最終行: （顧客数 + 1）
   最終列: 22 (V)   ← V列まで使われている
   ヘッダー行 (1行目):
     A列: "..."
     ...
     V列: "..."
   ```

**チェックポイント**：
- [ ] 最終列が **22 (V)** であることを確認（=既存はV列まで）
  - もし W列以降に既にデータがある場合は、**ここで止めて報告してください**

> 💡 最終列が V でない場合、列構成が変わっている可能性があります。Claude に「`checkClientListStatus` のログがこうでした」と貼り付けて確認を取ってください。

---

## ステップ5：セットアップ実行（1分）

1. 関数選択プルダウンで **`setupPriceGroupColumns`** を選びます。
2. **実行** ボタンをクリックします。
3. 実行ログに以下のメッセージが順番に出ることを確認：
   ```
   シート: Client list
   現在の最終行: ...
   ヘッダー設定: W1 = "Price Group"
   ヘッダー設定: X1 = "Markup %"
   プルダウン設定: W2:W... (...行)
   1行目までフリーズしました。
   ✅ セットアップ完了
   次のステップ: Group A 12社 / Individual 15社 の行のW列に値を入力してください。残りは "Standard" で一括埋めできます。
   ```
4. スプレッドシート側に **「セットアップ完了」のダイアログ**が表示されたら **OK** をクリック。

**チェックポイント**：
- [ ] ログに `✅ セットアップ完了` が出た
- [ ] エラーメッセージ（赤字）が出ていない

---

## ステップ6：結果確認（2分）

スプレッドシートに戻って、以下を確認します。

1. **W1セル**：青背景・白文字・太字で **`Price Group`** と表示されている
2. **X1セル**：青背景・白文字・太字で **`Markup %`** と表示されている
3. **W2セル**（データ1行目）をクリック：右下に▼マークが出てプルダウンが効いている
4. プルダウンを開くと **`Standard` / `Group A` / `Individual`** の3択が出る
5. 試しに `Standard` 以外のテキスト（例：`abc`）を入力 → **エラーで弾かれる**ことを確認
6. 1行目がフリーズされている（スクロールしてもヘッダーが固定）

**チェックポイント**：
- [ ] W列ヘッダーOK
- [ ] X列ヘッダーOK
- [ ] W列プルダウンOK
- [ ] 範囲外の値が拒否される
- [ ] フリーズOK

> ⚠ もしテスト入力（`abc` など）を弾く確認をしたら、**そのセルが空欄に戻っていることを確認**してください。

---

## ステップ7：完了報告（30秒）

すべてのチェックポイントが OK なら、Claude に以下を伝えてください：

> 「手順書1の setup_price_group.js 実行完了しました」

次は **手順書2（顧客分類入力）** に進みます。

---

## 困ったときは

### エラー：「シート "Client list" が見つかりません」
- シート名が違う可能性。タブ名の前後の半角スペースや大文字小文字を確認してください。

### エラー：「既に23列目以降に何か入っています。上書きされます。」
- W列以降に既にデータがある場合の警告です。**実行を止めて Claude に報告**してください。

### エラー：「権限が必要です」が消えない
- ブラウザのキャッシュをクリアして再ログインしてください。

### 「実行」ボタンが押せない
- スクリプトに構文エラーがある可能性。エディタ下部に赤字が出ていないか確認してください。
